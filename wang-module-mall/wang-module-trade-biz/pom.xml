<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <parent>
        <groupId>cn.zs.wang</groupId>
        <artifactId>wang-module-mall</artifactId>
        <version>${revision}</version>
    </parent>
    <modelVersion>4.0.0</modelVersion>
    <artifactId>wang-module-trade-biz</artifactId>
    <packaging>jar</packaging>

    <name>${project.artifactId}</name>
    <description>
        trade 模块，主要实现交易相关功能
        例如：订单、退款、购物车等功能。
    </description>

    <dependencies>
        <dependency>
            <groupId>cn.zs.wang</groupId>
            <artifactId>wang-module-trade-api</artifactId>
            <version>${revision}</version>
        </dependency>

        <dependency>
            <groupId>cn.zs.wang</groupId>
            <artifactId>wang-module-product-api</artifactId>
            <version>${revision}</version>
        </dependency>
        <dependency>
            <groupId>cn.zs.wang</groupId>
            <artifactId>wang-module-pay-api</artifactId>
            <version>${revision}</version>
        </dependency>
        <dependency>
            <groupId>cn.zs.wang</groupId>
            <artifactId>wang-module-promotion-api</artifactId>
            <version>${revision}</version>
        </dependency>
        <dependency>
            <groupId>cn.zs.wang</groupId>
            <artifactId>wang-module-member-api</artifactId>
            <version>${revision}</version>
        </dependency>

        <!-- 业务组件 -->
        <dependency>
            <groupId>cn.zs.wang</groupId>
            <artifactId>wang-spring-boot-starter-biz-operatelog</artifactId>
        </dependency>
        <dependency>
            <groupId>cn.zs.wang</groupId>
            <artifactId>wang-spring-boot-starter-biz-ip</artifactId>
        </dependency>

        <!-- Web 相关 -->
        <dependency>
            <groupId>cn.zs.wang</groupId>
            <artifactId>wang-spring-boot-starter-web</artifactId>
        </dependency>

        <dependency>
            <groupId>cn.zs.wang</groupId>
            <artifactId>wang-spring-boot-starter-security</artifactId>
        </dependency>

        <!-- TODO 芋艿：引入依赖要优化下： -->
        <dependency>
            <groupId>cn.zs.wang</groupId>
            <artifactId>wang-spring-boot-starter-biz-pay</artifactId>
        </dependency>

        <!-- DB 相关 -->
        <dependency>
            <groupId>cn.zs.wang</groupId>
            <artifactId>wang-spring-boot-starter-mybatis</artifactId>
        </dependency>

        <!-- Test 测试相关 -->
        <dependency>
            <groupId>cn.zs.wang</groupId>
            <artifactId>wang-spring-boot-starter-test</artifactId>
        </dependency>

        <!-- 工具类相关 -->
        <dependency>
            <groupId>cn.zs.wang</groupId>
            <artifactId>wang-spring-boot-starter-excel</artifactId>
        </dependency>
    </dependencies>

</project>
